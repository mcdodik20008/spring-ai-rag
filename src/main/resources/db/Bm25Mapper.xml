<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="mcdodik.springai.db.mybatis.mapper.Bm25Mapper">
    <select id="search" resultType="mcdodik.springai.db.entity.rag.Bm25Row">
        SELECT
        id,
        content,
        ts_rank_cd(tsv, websearch_to_tsquery('simple', #{q})) AS score
        FROM rag_chunks
        WHERE tsv @@ websearch_to_tsquery('simple', #{q})
        ORDER BY score DESC
        LIMIT #{topK}
    </select>
</mapper>