<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
    PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
    "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="mcdodik.springai.db.mybatis.mapper.ChatMessageMapper">

    <select id="findByConversationId" resultType="mcdodik.springai.db.entity.user.ChatMessageRecord">
        SELECT
            id,
            conversation_id as conversationId,
            message_type as messageType,
            content,
            created_at as createdAt
        FROM chat_messages
        WHERE conversation_id = #{conversationId}
        ORDER BY created_at ASC
    </select>

    <insert id="insert">
        INSERT INTO chat_messages (conversation_id, message_type, content)
        VALUES (#{conversationId}, #{messageType}::varchar, #{content})
    </insert>

    <delete id="deleteByConversationId">
        DELETE FROM chat_messages WHERE conversation_id = #{conversationId}
    </delete>

</mapper>
